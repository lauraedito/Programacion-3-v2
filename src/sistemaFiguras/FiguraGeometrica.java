package sistemaFiguras;

public class FiguraGeometrica {
	
	double area;
	double perimetro;
	
	
/*
 * import java.util.Scanner;

// Clase base FiguraGeometrica
abstract class FiguraGeometrica {
    // Métodos abstractos para calcular área y perímetro
    public abstract double calcularArea();
    public abstract double calcularPerimetro();
}

// Clase derivada Rectangulo
class Rectangulo extends FiguraGeometrica {
    private double base;
    private double altura;

    public Rectangulo(double base, double altura) {
        this.base = base;
        this.altura = altura;
    }

    // Implementación de los métodos para calcular área y perímetro del rectángulo
    @Override
    public double calcularArea() {
        return base * altura;
    }

    @Override
    public double calcularPerimetro() {
        return 2 * (base + altura);
    }
}

// Clase derivada Circulo
class Circulo extends FiguraGeometrica {
    private double radio;

    public Circulo(double radio) {
        this.radio = radio;
    }

    // Implementación de los métodos para calcular área y perímetro del círculo
    @Override
    public double calcularArea() {
        return Math.PI * radio * radio;
    }

    @Override
    public double calcularPerimetro() {
        return 2 * Math.PI * radio;
    }
}

// Ejemplo de uso
public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        // Pedir al usuario las dimensiones del rectángulo
        System.out.println("Ingrese la base del rectángulo:");
        double baseRectangulo = scanner.nextDouble();
        System.out.println("Ingrese la altura del rectángulo:");
        double alturaRectangulo = scanner.nextDouble();

        // Crear un rectángulo con las dimensiones proporcionadas por el usuario
        Rectangulo rectangulo = new Rectangulo(baseRectangulo, alturaRectangulo);
        System.out.println("Área del rectángulo: " + rectangulo.calcularArea());
        System.out.println("Perímetro del rectángulo: " + rectangulo.calcularPerimetro());

        // Pedir al usuario el radio del círculo
        System.out.println("Ingrese
 */
}
